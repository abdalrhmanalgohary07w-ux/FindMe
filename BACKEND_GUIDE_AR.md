# دليل شرح نظام الباك-إيند (FindMe Backend Guide)

هذا الملف يحتوي على شرح تفصيلي لكل جزء في النظام، وكيفية عمل البحث بالصور، وتفاصيل قاعدة البيانات.

---

## 📂 أولاً: شرح الملفات والأكواد بالتفصيل

### 1. مجلد `api/` (النظام البرمجي للتطبيق)

#### 📄 `models.py` (تعريف البيانات)
هذا الملف هو المسؤول عن شكل الجداول في قاعدة البيانات.
*   **كود `MissingPerson`**: يمثل الشخص المفقود. كل حقل (Field) فيه يمثل عموداً في قاعدة البيانات:
    *   `full_name`: يحفظ الاسم كـ نص.
    *   `age`: يحفظ العمر كـ رقم.
    *   `last_seen_date`: يحفظ وقت وتاريخ آخر ظهور تلقائياً.
    *   `image`: حقل خاص لرفع الصور، ويقوم بحفظها في مجلد `missing_persons/`.
*   **الفائدة**: يضمن أن كل البيانات المدخلة تتبع نفس النظام والترتيب.

#### 📄 `serializers.py` (المترجم)
الموبايل يتعامل بلغة `JSON` والداتابيز تتعامل بلغة `Python Objects`. هذا الملف هو المترجم بينهما.
*   **كود `MissingPersonSerializer`**: يأخذ كلاً من `model = MissingPerson` و `fields = [...]` ليقوم بتحويل البيانات تلقائياً.
*   **الفائدة**: بدون هذا الملف، لن يستطيع تطبيق الموبايل قراءة البيانات القادمة من السيرفر.

#### 📄 `views.py` (منطق العمل - Business Logic)
هذا هو أضخم وأهم ملف، يحتوي على الأكواد التي تنفذ الأوامر.
*   **`MissingPersonViewSet`**: كلاس جاهز يوفر عمليات (إضافة، تعديل، حذف، عرض) للأشخاص المفقودين.
*   **دالة `search_by_image`**:
    *   تستخدم مكتبة `DeepFace` للذكاء الاصطناعي.
    *   تستخدم `tempfile` لإنشاء ملف مؤقت للصورة المرفوعة.
    *   تحتوي على حلقة تكرار (`for loop`) تمر على كل الصور المخزنة وتقارنها بالصورة الحالية.
    *   تستخدم `distance_metric='cosine'` لقياس دقة التشابه.

#### 📄 `urls.py` (توجيه المسارات)
يحتوي على الـ `Router`.
*   **الوظيفة**: يربط الكلمات التي نكتبها في المتصفح أو الأبليكيشن (مثل `missing-persons`) بالأكواد الموجودة في `views.py`.

---

### 2. مجلد `findme_backend/` (الإدارة المركزية)

#### 📄 `settings.py` (الإعدادات)
*   **`INSTALLED_APPS`**: تخبر النظام أننا نستخدم مكتبات خارجية مثل `rest_framework`.
*   **`DATABASES`**: تحدد أننا نستخدم `sqlite3` ومكان ملفها.
*   **`REST_FRAMEWORK`**: هنا قمنا بضبط الصلاحيات ليكون السيرفر متاحاً للجميع (`AllowAny`) لتسهيل الاختبار.
*   **`MEDIA_ROOT`**: يحدد المجلد الفعلي على الهارد ديسك الذي ستخزن فيه صور المفقودين.

#### 📄 `urls.py` (البوابة الرئيسية)
*   يربط روابط مشروع الـ `api` بالروابط العامة للموقع.

---

### 3. ملفات مساعدة

#### 📄 `manage.py`
*   هو "جهاز التحكم عن بعد". نستخدمه لتشغيل السيرفر (`runserver`) أو تطبيق التغييرات على الداتابيز (`migrate`).

#### 📄 `requirements.txt`
*   كتالوج المكتبات. يخبر أي مبرمج أو سيرفر جديد ما هي البرامج التي يجب تثبيتها ليعمل المشروع (مثل `DeepFace`, `Django`, `Pillow`).

---

## 🔍 ثانياً: سيناريو البحث بالصورة (Step-by-Step)

عندما يقوم المستخدم برفع صورة للبحث عنها، يحدث الآتي خلف الكواليس في دالة `search_by_image`:

1.  **الاستقبال**: يستلم السيرفر الصورة من الموبايل عبر طلب `HTTP POST`.
2.  **التخزين المؤقت**: يتم حفظ الصورة المرفوعة في ملف مؤقت (`TemporaryFile`) على السيرفر لكي نتمكن من تحليلها برمجياً.
3.  **جلب البيانات**: يقوم الكود بجلب جميع الأشخاص المفقودين من الداتا بيز الذين يمتلكون صوراً مخزنة.
4.  **المقارنة الحيوية (Deep Learning)**:
    *   نستخدم مكتبة `DeepFace`.
    *   نحدد موديل `Facenet512` وهو موديل ذكاء اصطناعي قوي جداً في تمييز ملامح الوجه.
    *   يتم عمل `DeepFace.verify` بين الصورة المرفوعة وكل صورة في الداتا بيز.
5.  **حساب المسافة (Distance)**:
    *   المحرك يعطي "مسافة" (Cosine Distance).
    *   كلما كانت المسافة أقرب للصفر، كان التشابه أكبر.
6.  **الفلترة**:
    *   أي صورة المسافة بينها وبين صورة البحث أقل من `0.6` نعتبرها "تشابه محتمل".
    *   يتم ترتيب النتائج من الأكثر شبهاً (أقل مسافة) إلى الأقل شبهاً.
7.  **الرد**: يتم إرسال قائمة البيانات (JSON) إلى الموبايل ليعرضها للمستخدم.

---

## 🗄️ ثالثاً: قاعدة البيانات المحلية (SQLite)

### كيف تم إنشاؤها؟
1.  **ORM (Object Relational Mapping)**: في Django، لا نكتب الأوامر بـ SQL مباشرة، بل نكتب "كلاسات" في `models.py`.
2.  **Migrations**:
    *   عند كتابة الموديل، قمنا بتنفيذ `python manage.py makemigrations` الذي حلل الكود وكتب خطة البناء.
    *   ثم نفذنا `python manage.py migrate` الذي قام بإنشاء الجداول فعلياً داخل ملف `db.sqlite3`.

### هيكل الجدول:
الجدول `api_missingperson` يحتوي على أعمدة متوافقة مع الحقول المعرفة في الكود، مع إضافة عمود تلقائي للـ `id` (الرقم التعريفي) وعمود `created_at` لمعرفة وقت إضافة البلاغ.

---

**ملاحظة**: الصور لا تُخزن داخل الداتا بيز كملفات، بل يُخزن "مسارها" فقط، بينما يتم حفظ الملف الفعلي في مجلد `media/missing_persons/`.

---

## 💡 قصة المشروع ببساطة (للطالب)

تخيّل أن الأبليكيشن ده عبارة عن **مطعم كبير**:

1.  **الموبايل (Frontend)**: هو "الجرسون" اللي بياخد الطلب من الزبون (المستخدم) ويروح يوديه للمطبخ.
2.  **الباك-إيند (Backend)**: هو "المطبخ" اللي بيجهز الأكل. في حالتنا دي، المطبخ بيجهز المعلومات.
3.  **الداتا بيز (Database)**: هي "الثلاجة" أو المخزن اللي بنشيل فيه الخضار (بيانات الناس) عشان نطلعه وقت اللزوم.
4.  **الـ AI (DeepFace)**: هو "خبير" واقف جوه المطبخ، لما الجرسون يجيب صورة، الخبير ده بيمسكها ويقارنها بكل الصور اللي في الثلاجة عشان يقولنا "ده شبه ده بنسبة كام".

### ليه استخدمنا الحاجات دي بالذات؟
*   **بايثون (Python)**: لأنها أشهر لغة في العالم بتفهم ذكاء اصطناعي، وسهلة في القراءة.
*   **دجانغو (Django)**: لأنه بيخلينا نبني "المطبخ" بسرعة وبشكل منظم جداً، وبيحمي البيانات من الاختراق.
*   **DeepFace**: دي مكتبة جاهزة "شافت" ملايين الوجوه قبل كده، فبدل ما إحنا نعلّم السيرفر إزاي يعرف الوش من الصفر، إحنا بنستدعي "الخبير" ده وهو بيقوم بالمهمة فوراً.

### ملخص العملية:
لما بتبعت صورة، السيرفر مش بيقول "دي صورة فلان"، هو بيحوّل الصورة لـ **أرقام** تمثل ملامح الوش، ويقارن الأرقام دي بالأرقام اللي عنده في الداتا بيز. لو الأرقام قريبة جداً من بعض، بيبعت النتيجة للموبايل.

